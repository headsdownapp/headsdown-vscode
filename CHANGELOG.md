# Changelog

## 0.1.0 (Unreleased)

Initial release.

- Status bar indicator showing availability mode, status text, and time remaining
- Device Flow authentication with browser auto-open
- Unauthenticated coding timer (shows session duration before sign-in)
- Configurable settings via VS Code Settings UI and `~/.config/headsdown/config.json`
- Timestamped output channel for debugging
- Quick Actions menu (Cmd+Shift+H / Ctrl+Shift+H)
- First-install notification delayed 24h to let auto-detection fire first
- Cursor integration snippet in README
